{
  "scm-tagProtection-plugin": {
    "nav-link": "Git Tag Protection",
    "config": {
      "form": {
        "header": "Git Tag Protection",
        "protectionPattern": "Protection pattern",
        "protectionPattern-helptext": "A wildcard pattern to describe the tags that are protected from removal. Use * or ? as wildcards.",
        "reduceOwnerPrivilege": "Reduce owner privilege",
        "reduceOwnerPrivilege-helptext": "Users with the permission to modify repositories (i. e. owners) are allowed to remove any tag in their repositories by default. However, when checked they may only remove tags that are not protected, i. e. like regular users."
      }
    }
  }
}
